[tool.poetry]
name = "autoresearch-supervised"
version = "0.1.0"
description = "Supervised training protocol for autoresearch"
authors = []
packages = [{ include = "autoresearch/supervised" }]

[tool.poetry.dependencies]
python = ">=3.11,<4.0"
torch = ">=2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
